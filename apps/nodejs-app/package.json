{
  "name": "multicloud-nodejs-app",
  "version": "1.0.0",
  "description": "Sample Node.js application for multi-cloud deployment",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "jest",
    "build": "echo 'No build step required for Node.js'",
    "docker:build": "docker build -t multicloud-nodejs-app .",
    "docker:run": "docker run -p 3000:3000 multicloud-nodejs-app"
  },
  "keywords": ["nodejs", "multicloud", "kubernetes"],
  "author": "MultiCloud Team",
  "license": "MIT",
  "dependencies": {
    "express": "^5.0.0",
    "cors": "^2.8.6",
    "helmet": "^8.0.0",
    "morgan": "^1.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "jest": "^30.0.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}